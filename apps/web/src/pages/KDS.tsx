import { StationBoard } from '../components/StationBoard';

/** Kitchen Display System — food tickets only (bar is on its own board). */
export function KDS() {
  return <StationBoard station="kitchen" title="Kitchen display" />;
}
